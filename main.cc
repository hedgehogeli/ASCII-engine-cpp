#include "engine.h"
#include <string>
#include <iostream>

int main(int argc, char* argv[]) {
    // init Engine
    Engine e(78, 20);

    e.render();

    // Engine.play():
    // while (true) {
    //     timeout(50);

    //     getch();
    // }
}